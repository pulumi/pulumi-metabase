{
    "name": "@pulumi/metabase",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource metabase ${VERSION}"
    },
    "dependencies": {
        "@pulumi/aws": "^5.0.0",
        "@pulumi/random": "^4.0.0"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
